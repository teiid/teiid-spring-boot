        setServer(this.server);
        setVdb(this.vdb);
        LinkedHashMap<String, Object> parameters = new LinkedHashMap<String, Object>();
        {{#allParams}}
        parameters.put("{{paramName}}",{{paramName}});
        {{/allParams}}
        {{#hasReturn}}
        return execute("{{operationId}}", parameters, "UTF-8", {{hasReturn}});        
        {{/hasReturn}}
        {{^hasReturn}}
        execute("{{operationId}}", parameters, "UTF-8", {{hasReturn}});
        return new ResponseEntity<>(HttpStatus.OK);        
        {{/hasReturn}}