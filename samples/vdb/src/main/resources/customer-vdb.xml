<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<vdb name="customer" version="1">

    <description>Customer VDB</description>

    <model name="customer">
        <property name="importer.useFullSchemaName" value="false"/>
        
        
        <property name="connection.url" value="${foo.env}"/>
        <property name="connection.username" value="${bar.env}"/>
        <property name="connection.password" value="false"/>
        
        <source name="mydb" translator-name="h2" connection-jndi-name="mydb"/>
    </model>

</vdb>
